language: php

# list any PHP version you want to test against
php:
  - 5.4
  - 5.6
  - 7.0
  - hhvm

# omitting "script:" will default to phpunit
# use the $DB env variable to determine the phpunit.xml to use
script: phpunit ./app/tests
